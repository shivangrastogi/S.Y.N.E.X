# ── Audio I/O ────────────────────────────────────────────────────────
SpeechRecognition          # online STT wrapper (Google en-IN)
vosk                       # offline STT + wake-word detection
pyaudio                    # mic input
edge-tts                   # online TTS (Hindi Neerja voice)
pyttsx3                    # offline TTS final fallback
pygame                     # audio playback

# ── NLP / Brain ──────────────────────────────────────────────────────
sentence-transformers      # multilingual encoder           [C1]
scikit-learn               # k-NN intent index              [C1]
transformers               # fine-tuned XLM-R intent head   [C13 — run: python -m core.train_intent]
spacy                      # NER + tokenisation             [C2 — optional, run: python -m spacy download en_core_web_sm]
vaderSentiment             # sentiment v1                   [C3]
rapidfuzz                  # fuzzy fallback (intent_engine.py)

# ── LLM (chit-chat + tool calling) ───────────────────────────────────
requests                   # Ollama HTTP client             [C6, G1]

# ── Scheduling + Crypto + Plugin runtime ─────────────────────────────
apscheduler                # real reminders                 [F3]
cryptography               # AES-256 memory vault           [G11]

# ── UI / system ──────────────────────────────────────────────────────
PyQt5
psutil
keyboard
pyautogui
opencv-python              # gesture (utils/gesture.py)
mediapipe                  # gesture (utils/gesture.py)

# ── Plugin skill optional dependencies ───────────────────────────────
# Each is loaded lazily by its skill module — not installing it just
# disables that one skill. The rest of AERIS keeps working.
pyperclip                  # skills/clipboard.py + skills/clipboard_history.py
feedparser                 # skills/news.py
beautifulsoup4             # skills/web_search.py        (HTML parsing)
lxml                       # skills/web_search.py        (faster bs4 backend)
ddgs                       # skills/web_search.py        (no-API search; replaces duckduckgo-search)
openpyxl                   # skills/expense_tracker.py   (.xlsx + charts)
gspread                    # skills/sheets_sync.py       (Google Sheets push)
google-auth                # skills/sheets_sync.py       (service account)
ultralytics                # core/object_detector.py     (YOLOv8n; falls back to mediapipe)
win10toast                 # skills/timer_skill.py       (toast notification on fire)
mss                        # skills/snip_ocr.py + skills/vision.py
pytesseract                # skills/snip_ocr.py          (also needs Tesseract binary)
Pillow                     # skills/snip_ocr.py
# pywhatkit                # skills/whatsapp.py    (uses WhatsApp Web)
# argostranslate           # skills/translate.py   (offline Hi↔En)
# pytesseract              # skills/vision.py      (also needs Tesseract binary)
# mss                      # skills/vision.py
# Pillow                   # skills/vision.py
# spotipy                  # skills/spotify_control.py

# ── Misc ─────────────────────────────────────────────────────────────
pyyaml
nltk

# ── Dev / test ───────────────────────────────────────────────────────
pytest                     # test runner                    [C11]

# ── REMOVED ──────────────────────────────────────────────────────────
# tensorflow               # replaced by k-NN index in C1
